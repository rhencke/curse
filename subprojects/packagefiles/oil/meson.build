# Source-only overlay: curse runs Oils' spec cases that target bash.
# Exposes the spec/ dir to the parent (see the conformance tests in ../../meson.build).
project('oil-spec', 'c')
spec_dir = meson.current_source_dir() / 'spec'
